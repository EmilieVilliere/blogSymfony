{% extends "base.html.twig" %}

{% block body %}

        <h1>{{ tag.name }}</h1>
        {% for article in articles %}
            <h2>{{ article.id }} {{ article.title }}</h2>
            {{ article.content }}
        {% endfor %}

{% endblock %}