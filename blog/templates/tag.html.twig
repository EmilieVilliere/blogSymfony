{% extends "base.html.twig" %}

{% block body %}


    <div>

        <h1>Tag article : {{ tag.name }}</h1>

        {% for article in articles %}
            <h2>Title : {{ article.id }} {{ article.title }}</h2>
            <p>{{ article.content }}</p>
        {% endfor %}
    </div>

{% endblock %}